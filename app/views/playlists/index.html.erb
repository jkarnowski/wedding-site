<div class="container">
	<div class="row">
		<div class="col span-12-d debug">
			<h2>Potential Playlist:</h2>
			<% @playlists.each do |song| %>
				<p><%= song.song %></p>
			<% end  %>
		</div>
	</div>
</div>